var should = require('should');
var queryresult = require('../lib/queryresult');


describe('queryresult', function() {
  it('convertes a to b', function() {
    (true).should.not.be.fale;
  });

  it('returns a', function() {
    (true).should.be.true;
  });
});
