html
  head
    title!= title
  body
    h1 Search patterns on cloned repos
    form(name="search", action="/search/commits", method="post")
      input(name="regex", placeholder="regex", style="width: 300")
      input(name="ext", placeholder="ext")
      input(name="max", placeholder="max")
      input(type="submit", value="search")
    p Example:
    ul
      li regex: .setTag\([^,|^\(]*,[^,]*\)
      li ext: java
      li max: 10
    a(href='http://regexr.com/') Practice Regex.
