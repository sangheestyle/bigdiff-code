html
  head
    title!= title
  body
    h1 Search patterns on cloned repos
    p e.g., regex: .setTag\([^,|^\(]*,[^,]*\) ext: java
    form(name="search", action="/search/commits", method="post")
      input(type="text", name="regex", placeholder="regex")
      input(type="text", name="ext", placeholder="ext")
      input(type="submit", value="search")
    a(href='http://regexr.com/') Practice Regex.
